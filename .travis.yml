language: ruby
rvm:
- 2.3.1

before_install:
- gem install bundler
- bundle

install:
- bundle install

deploy:
  provider: pages
  github-token: $GITHUB_TOKEN
  local-dir: _site
  target-branch: gh-pages
  skip-cleanup: true
  keep-history: true
  on:
    branch: master
